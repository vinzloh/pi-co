---
description: Read hunk skill, and review current session
---

## Prerequisites

- require `hunk`. Check `hunk --version`. If missing, ask the user to install and stop here.
- there should active hunk session - check `hunk session list`. If missing, ask the user start a session and stop here.

## Workflow

- read hunk skill by running `!hunk skill path`, then review current session
